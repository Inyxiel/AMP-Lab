# AMP Lab v7.3.5 Baseline

Repository baseline: **AMP Lab v7.3.5 — Tooltip Restore Fix**.

This baseline was taken from the supplied Windows executable only. No older AMP Lab source snapshot is used to fill missing backend code.

## Verified binary identity

- Filename: `AMP20Lab_v7.3.5_Tooltip-Restore-Fix.exe`
- Size: `36,457,984` bytes
- SHA-256: `2b7f75480c27072c9447624aa6356c9cc217f922cc818b95a20766e188c2ae5c`

## Recoverable from the executable

The binary contains an embedded HTML document beginning with `<!doctype html>` and ending with `</html>`. It also contains a gzip-compressed JavaScript payload encoded with base64 inside that HTML. The repository recovery script extracts these resources directly from the v7.3.5 binary.

## Not claimed as recovered source

The executable is a compiled Go program. The original Go source files are not present as source text in the binary. Until the backend is reconstructed and validated against v7.3.5 behavior, the repository must not claim that it can reproduce the exact EXE from source.

## Data policy

AMP/D&D `.pak` files, AMPIS exports, spreadsheets, save files and runtime snapshots are local inputs and are not committed here. This avoids accidentally redistributing third-party assets or private player data.
