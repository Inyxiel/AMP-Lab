# AMP Lab v7.4.8 Baseline

Repository baseline: **AMP Lab v7.4.8 — Raw Arsenal / Direct Shards**.

This baseline was taken from the supplied Windows executable only. No older AMP Lab source snapshot is used to fill missing backend code.

## Verified binary identity

- Filename: `AMP20Lab_v7.4.8_Raw-Arsenal-Direct-Shards.exe`
- Size: `36,457,984` bytes
- SHA-256: `b4367fc3ab6c46123c31b19d2f795e5b765e079179d618b954dcc19ca1ccd571`

## Recoverable from the executable

The binary contains an embedded HTML document beginning with `<!doctype html>` and ending with `</html>`. It also contains one gzip-compressed JavaScript payload encoded with base64 inside that HTML.

Verified extraction from the supplied v7.4.8 binary:

- Embedded HTML size: `126,137` bytes
- Decompressed JavaScript size: `261,067` bytes
- The embedded HTML identifies itself as `v7.4.8`

The repository recovery script extracts these resources directly from this exact binary.

## Not claimed as recovered source

The executable is a compiled Go program. The original Go source files are not present as source text in the binary. Until the backend is reconstructed and validated against v7.4.8 behavior, the repository must not claim that it can reproduce the exact EXE from source.

## Data policy

AMP/D&D `.pak` files, AMPIS exports, spreadsheets, save files and runtime snapshots are local inputs and are not committed here. This avoids accidentally redistributing third-party assets or private player data.
